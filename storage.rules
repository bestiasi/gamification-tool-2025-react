rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Storage rules removed - not using Firebase Storage on free tier
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
